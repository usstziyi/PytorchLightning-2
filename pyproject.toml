[project]
name = "pytorch-lightning-learning"
version = "0.1.0"
description = "PyTorch Lightning 循序渐进学习计划与实战项目"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.1",
    "torchvision>=0.16",
    "lightning>=2.2",
    "tensorboard>=2.15",
]

[tool.uv]
# 本仓库仅作为学习脚本集合，不打包成可安装的库
package = false